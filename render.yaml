services:
  - type: web
    name: logitrack
    runtime: node
    buildCommand: npm install && pip install -r ml/requirements.txt
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PYTHON_BIN
        value: python3
